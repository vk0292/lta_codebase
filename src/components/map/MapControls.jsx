// Map Component: MapControls

import React from 'react';

const MapControls = () => {
  return (
    <div className="map-controls">
      <button>Zoom In</button>
      <button>Zoom Out</button>
      <button>Reset View</button>
    </div>
  );
};

export default MapControls;
